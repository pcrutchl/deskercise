# Local runtime state lives in ~/.deskercise, but ignore these if anyone points
# state_dir at the repo, plus the usual noise.
.deskercise/
*.log
__pycache__/
*.pyc
.DS_Store
.venv/
.pytest_cache/

# Exercise reference images are kept local (avoids committing copyrighted images
# to the public repo). media/README.md is still tracked.
*.png
*.jpg
*.jpeg
*.gif
*.webp
